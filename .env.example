DATABASE_URL=postgres://meal_hq:meal_hq@localhost:5433/meal_hq
# Generate via: openssl rand -hex 32  (or: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
COOKIE_SECRET=replace-with-32-byte-hex-secret
BCRYPT_ROUNDS=12
TZ=Europe/Athens
